def columns(age):
    #pull columns based on age

def salaryFormat(salary, columnList):
    #format outputs based on salary and percentages

def sliderCalculation(columnNumber, sliderPercentage, salary, columnList):
    #adjust other columns based on given columnNumber sliderPercentage
